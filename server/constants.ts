// Subset of the original src/constants/custom.ts — only what the auth
// email templates actually use. If you copy over the rest of the API
// (jobs, queues, other domain constants), merge this into that larger
// constants module instead of keeping two.

export const APP_NAME = "Ini";
export const APP_LOGO = "";
export const COMPANY_ADDRESS = "Abuja, Nigeria";
